CREATE TABLE Customers(
	CustomerID INTEGER PRIMARY KEY,
	Name TEXT NOT NULL
);
CREATE TABLE Orders(
  OrderID INTEGER PRIMARY KEY,
  Date VARCHAR(10) NOT NULL,
  CustomerID INTEGER NOT NULL,
  FOREIGN KEY(CustomerID) REFERENCES Customers(CustomerID)
);
CREATE TABLE LineItems (
    itemID INTEGER PRIMARY KEY,
    OrderID INTEGER NOT NULL,
    Price INTEGER NOT NULL,
    FOREIGN KEY(OrderID) REFERENCES Orders(OrderID)
);
INSERT INTO Customers (Name) VALUES('DrKow');
INSERT INTO Customers (Name) VALUES('Plato');
INSERT INTO Customers (Name) VALUES('Socrates');
INSERT INTO Orders VALUES(NULL, '2024-10-1', 2);
INSERT INTO Orders VALUES(NULL, '2024-10-10', 1);
INSERT INTO Orders VALUES(NULL, '2024-10-16', 1);
INSERT INTO LineItems VALUES(NULL, 1, 16500);
INSERT INTO LineItems VALUES(NULL, 1, 2295);
INSERT INTO LineItems VALUES(NULL, 1, 2295);
INSERT INTO LineItems VALUES(NULL, 2, 1299);
INSERT INTO LineItems VALUES(NULL, 2, 1099);
INSERT INTO LineItems VALUES(NULL, 3, 2295);
SELECT * FROM LineItems WHERE OrderID = 1;
SELECT SUM(Price) FROM LineItems WHERE OrderID = 1;
SELECT Date FROM Orders WHERE CustomerID = 1;
SELECT Customers.name,Orders.date FROM Orders INNER JOIN Customers ON Orders.CustomerID=Customers.CustomerID;
SELECT Customers.name,Orders.Date FROM Customers LEFT JOIN Orders ON Orders.CustomerID=Customers.CustomerID;
UPDATE LineItems SET Price=899 WHERE itemID=5;
DELETE FROM LineItems WHERE itemID = 3;